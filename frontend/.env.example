# Only needed if you delete the functions/ folder and host without Pages Functions.
# On Cloudflare Pages set DISCORD_WEBHOOK_URL as an encrypted environment
# variable instead, which keeps it off the client entirely.
#
# Anything set here is compiled into the public JavaScript bundle, so treat it
# as visible to anyone who views source.
VITE_DISCORD_WEBHOOK_URL=
